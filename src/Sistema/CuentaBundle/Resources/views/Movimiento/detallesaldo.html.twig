{% extends '::basecuerpo1CG.html.twig' %}

{% block title %}Generar Pedido{% endblock %}
{% block navigation %}      
    <nav>
        <ul class="navigation">
            <li><a href="{{ path('cuenta_inicioAlu')}}">Volver</a></li>                
        </ul>
    </nav>
{% endblock %}
{% block saludo %}
<p>Conoctado como "{{app.user.tipou}}":</p>
<p id="us">
{{app.user.username}}
</p>
{#<a href="#">Ver mi perfil</a>#}
<p id="tu" style="display:none">{{app.user.tipou}}</p>
{% endblock %}
{% block maincol %}
    <div class="continfo">
        {% set tot = 0%}
        {% set saldond = 0%} 
        {% set totpen = 0%}
        {% set saldod = 0%}    
        {%for dato in pedidoadeudado%}
            {% set tot = tot + dato.total%}
            {%set totpen = dato.total/2%}
            {% set saldond = saldond + totpen%}
        {%endfor%}
        {%set saldod = saldoactual - saldond %}    
        <div class="continfosal">
            <div class="col2"><p>Saldo en Cuenta: ${{saldoactual}}</p></div>
            <div class="col2"><p>Saldo Disponible: ${{saldod}}</p></div>
            <div class="col2"><p>Saldo No Disponible: ${{saldond}}</p></div>
        </div>
        <div class="continfodesc">
            <div class="col1"><p>Saldo en Cuenta:</p></div>
            <div class="col1"><p>Importe total de la Cta. Cte.</p></div>
            <div class="col1"><p>Saldo Disponible:</p></div>
            <div class="col1"><p>Es el saldo que se encuentra en tu cuenta y puede ser usado para pagar pedidos nuevos.</p></div>
            <div class="col1"><p>Saldo No Disponible:</p></div>
            <div class="col1"><p>Es el saldo que se encuentra reflejado en la cuenta pero ya esta destinado para realizar el pago de pedidos realizados(predidos adeudados).</p></div>            
        </div>


    </div>
{% endblock %}
{% block javascripts %} 

{% endblock%}
    
    
    
